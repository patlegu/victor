# Victor — dépendances runtime
spacy>=3.7.0

# Validation automatique des gaps (optionnel)
# Nécessite Ollama : https://ollama.com/install
# ollama pull qwen2.5:1.5b

# Modèle AnonyNER (recommandé — entités cyber)
# pip install https://github.com/patlegu/anonyfiles/releases/download/anonyner-v3.0.0/en_anonyner-3.0.0.tar.gz

# Modèle spaCy générique (fallback si AnonyNER absent)
# python -m spacy download en_core_web_md

# Dépendances de développement
# pytest>=8.0
# pytest-cov

# ── Entraînement AnonyNER (training/ uniquement) ──────────────────────────────
# GPU tok2vec (requis par thinc pour spacy train --gpu-id)
# Note : cupy-cuda12x==14.x incompatible avec thinc 8.3.x (bridge DLPack cassé)
# pip install cupy-cuda12x==13.3.0
#
# GPU transformer (roberta-base ou autre HuggingFace)
# pip install torch --index-url https://download.pytorch.org/whl/cu121
# pip install spacy-transformers>=1.4.0
#
# Lecture archives .7z (dataset KernelDriver)
# pip install py7zr
